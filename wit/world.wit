package numcodecs:abc@0.1.1;

world imports {
    import codec;
    @unstable(feature = registry)
    export registry;
}

world exports {
    export codec;
    @unstable(feature = registry)
    import registry;
}
